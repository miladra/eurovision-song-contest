apply plugin: "war"

bootWar {
    mainClass = "com.amazing.vote.VoteApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
