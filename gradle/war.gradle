apply plugin: "war"

bootWar {
    mainClass = "com.pegasystems.vote.VoteApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
